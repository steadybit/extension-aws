name: ghcr-cleanup

on:
  workflow_dispatch: { }
  schedule:
    - cron: "0 8 * * 1"

jobs:
  ghcr-cleanup:
    uses: steadybit/extension-kit/.github/workflows/reusable-ghcr-cleanup.yml@main # NOSONAR githubactions:S7637 - our own action
    secrets:
      token: ${{ secrets.GHCR_CLEANUP_PAT }}
